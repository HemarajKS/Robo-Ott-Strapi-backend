'use strict';

/**
 * subscription-label service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::subscription-label.subscription-label');
